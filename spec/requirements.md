# MANDATE — Requirements Registry

The design vision lives in [DESIGN.md](./DESIGN.md). This file decomposes it into
small, addressable requirements with stable IDs. Tests reference these IDs in
comments (e.g. `// SPEC-CRED-1`), and `npm run spec:trace` fails CI if any
requirement tagged `[testable]` has no referencing test.

Tag meanings: `[testable]` = must be covered by an automated test;
`[design]` = narrative/UX intent, verified by review rather than tests.

## Simulation core
- **SPEC-SIM-1** `[testable]` The simulation is deterministic: same seed + inputs reproduce a run exactly. Engine code never calls `Math.random()` or reads the wall clock.
- **SPEC-SIM-2** `[design]` The full financial network is the single source of truth; all dashboards and the fractal-zoom map are read-only views derived from it.
- **SPEC-SIM-3** `[testable]` A pure `tick(state, months)` advances `state.date` by N months on the `YYYY-MM` calendar and maintains a bounded `state.history: GameStateSnapshot[]` (size from `content/engine/params.json#tick.history_size`). By convention `state.history` excludes the current state; `history[0]` is the most-recent prior snapshot (`date − 1 month` after a 1-month tick). The function returns a new `GameState` and does not mutate the input.
- **SPEC-SIM-6** `[testable]` The simulation tick cadence is parameterised via `content/engine/clock-cadence.json` (`schemas/clock-cadence.schema.json`). `ticks_per_month` controls how many dynamics steps run per calendar month. `scaleParamsForTick(params, n)` in `src/engine/cadence.ts` re-expresses monthly `MacroDynamicsParams` as per-tick params: `inflation_persistence` uses power scaling (`p^(1/n)`), `unemployment_adjustment_speed` uses exact discrete-time scaling (`1−(1−α)^(1/n)`), and flow contributions (`phillips_slope`, `expectations_adaptivity`, `expectations_anchor_pull`, `credibility_mission_gain`) are divided by n. `Session.advance()` uses the cadence to run `ticks_per_month` sub-tick dynamics steps per calendar month; the FOMC meeting schedule remains month-based; `months_below_anchor` increments by at most 1 per calendar month regardless of cadence. Documented tolerance: monthly and weekly (n=4) trajectories agree within 0.2pp (20 bp) for `inflation`, `unemployment`, and `expectations_anchor` over 36 months.
- **SPEC-SIM-4** `[testable]` A **replay** is a schema-governed content artifact under `content/replays/` capturing the **player's strategy** — a structured sequence of `{date, policy_rate, ...}` actions, with no engine-computed values. The engine-side runner `runReplay(replayId, months)` in `src/engine/replay.ts` applies the strategy to the engine via `tick` and returns an **in-memory** trajectory (not committed; engine-computed values stay in memory). Two consecutive `runReplay` calls produce bit-identical trajectories. Replay actions with dates outside the simulated `[0, months)` window are surfaced via `UnconsumedReplayActionsError` so author typos don't silently produce mis-aligned trajectories. At least one committed replay (`content/replays/1979_chair_tightening.json`, id `replay.1979_chair_tightening`) is loadable via `loadReplay`.

## Credibility & expectations
*`SPEC-CRED-1`/`SPEC-CRED-4` were redesigned (issue #33 + the realistic-dynamics calibration): dissent no longer costs credibility, credibility is tied to the mandate mission (`SPEC-CRED-6`), and the binary de-anchoring spiral became a continuous credibility-weighted expectations model.*

- **SPEC-CRED-1** `[testable]` Market surprises reduce credibility and on-target outcomes raise it; the value is clamped to [0, 100]. Committee **dissents do not change credibility** — FOMC dissents are not published in a way that damages the Chair's standing, so `applyMeetingOutcome` ignores the dissent count for credibility purposes; consensus-building costs are modelled separately (issue #33).
- **SPEC-CRED-2** `[testable]` Inflation expectations remain anchored only at or above a credibility threshold.
- **SPEC-CRED-3** `[testable]` Lower credibility increases the "pain multiplier" on policy moves (1x at full credibility up to 3x at zero).
- **SPEC-CRED-4** `[testable]` Within `applyMacroDynamics`, inflation **expectations** (`expectations_anchor`) evolve continuously each month, weighted by credibility (replacing the slice-1 binary spiral). With `c = credibility / 100`: `expectations_anchor += expectations_adaptivity * (1 - c) * (inflation - expectations_anchor) - expectations_anchor_pull * c * (expectations_anchor - target_inflation)`. When credibility is low, expectations track recent realized inflation (adaptive — they de-anchor in whatever direction inflation is moving); when credibility is high, they are pulled toward `target_inflation` (re-anchoring), which historically delivered roughly half the disinflation. `state.vars.months_below_anchor` still increments each month that `credibility < anchor_threshold` and is **frozen, not reset** on recovery, preserving the persistent-memory stat surfaced by the UI. Params (`expectations_adaptivity`, `expectations_anchor_pull`, `target_inflation`, `anchor_threshold`) live in `content/engine/credibility.json` (schema-governed).
- **SPEC-CRED-6** `[testable]` Credibility is tied to the **mandate mission**, not committee voting. Within `applyMacroDynamics`, define the dual-mandate distance `D = |inflation - target_inflation| + credibility_unemployment_weight * |unemployment - unemployment_target|`. Credibility changes by `credibility_mission_gain * (D_before - D_after)` for the month's move (using the pre- and post-update inflation/unemployment) and is clamped to [0, 100]: it **rises when the economy moves toward target and falls when it moves away**. `credibility_unemployment_weight < 1` makes disinflation progress dominate, so winning the inflation fight builds credibility even through a policy-induced recession. Params (`credibility_mission_gain`, `credibility_unemployment_weight`, `unemployment_target`, `target_inflation`) live in `content/engine/credibility.json` (schema-governed). Implements issue #33.
- **SPEC-CRED-5** `[testable]` Credibility bounds and meeting-outcome weights are content, not code: `cred_min`, `cred_max`, `surprise_penalty`, and `on_target_gain` live in `content/engine/credibility.json` (schema-governed by `schemas/credibility.schema.json`). `src/engine/credibility.ts` loads them through the validated content loader; the exported `CRED_MIN`/`CRED_MAX` constants and `applyMeetingOutcome` (which subtracts `surprise_penalty` when `surprisedMarkets` and adds `on_target_gain` when `onTarget`) take their values from that file, with no hardcoded copies in engine code. Tests assert the engine's observed deltas and bounds equal the content-file values.

## Content language
- **SPEC-COND-1** `[testable]` Conditions support all/any/not combinators, numeric variable comparisons, and boolean flag checks, evaluated against game state.
- **SPEC-COND-2** `[testable]` Effects are pure: applying them returns a new state and never mutates the input.

## Content integrity
- **SPEC-CONTENT-1** `[testable]` All shipped content (events, techs) validates against its JSON Schema before the engine loads it.
- **SPEC-CONTENT-2** `[design]` All player-facing strings are referenced by localization key; no display text appears in logic files.
- **SPEC-CONTENT-3** `[testable]` Named people in the game (committee members, characters) MUST use randomly-generated names. Content files MUST NOT contain ids whose slug matches a blocklist of real historical or famous public figures (e.g., FOMC chairs from any era, well-known economists). Enforced by `test/content-lint.test.ts` which walks `content/**/*.json` and asserts every id passes the blocklist.
- **SPEC-CONTENT-4** `[testable]` The shipped-content validation test covers every content directory, not just events and tech: `test/content.test.ts` loads `content/scenarios` against `schemas/scenario.schema.json`, `content/doctrines` against `schemas/doctrine.schema.json`, `content/briefings` against `schemas/briefing.schema.json`, and `content/hearings` against `schemas/hearing.schema.json` via `loadValidated`, asserting each directory yields at least one validated file.

## Institution & governance
- **SPEC-GOV-1** `[design]` The Chair cannot be removed except for cause; the soft continue-gate is 4-year reappointment, not firing.
- **SPEC-GOV-2** `[design]` The Board turns over slowly (one 14-year seat ~every two years); FOMC voters include a yearly-rotating subset of regional presidents.

## Scenarios
- **SPEC-SCEN-1** `[testable]` A scenario schema (`schemas/scenario.schema.json`) and loader (`src/content/scenarios.ts`) produce an initial `GameState` from a content file (`content/scenarios/*.json`). Loader accepts an optional `requiredVars: string[]` and throws `MissingVarsError` listing absent keys — preventing the silent-default-to-0 failure mode. Schema constrains `name`/`desc` to localization-key shape `^[a-z][a-z0-9_.]+$` so inline player-facing strings fail `npm run validate`. The `scen.1979_stagflation` scenario loads cleanly and has `date == "1979-08"`, `history: []`, and the slice-1 required vars present.
- **SPEC-SCEN-2** `[testable]` At least two additional authored starting scenarios beyond `scen.1979_stagflation` ship as content files in `content/scenarios/`. Each passes `npm run validate` against `schemas/scenario.schema.json`. `Session.fromScenario(id)` boots cleanly from each scenario id, and the resulting initial state matches the scenario's authored macro vars (inflation, unemployment, credibility, policy_rate, expectations_anchor). Each new scenario differs from the 1979 baseline on all five required macro vars. No real person names appear in any scenario content.

## Data fog
- **SPEC-FOG-1** `[testable]` A pure `observe(state, seriesId, rng)` returns a fogged view of the true state variable, with `noise_scale` and `lag_months` parameters from `content/engine/params.json#fog[seriesId]` (validated by `schemas/engine-params.schema.json`). Lag indexing: `lag_months === 0` reads the current `state.vars[seriesId]`; `lag_months >= 1` reads `state.history[lag_months - 1].vars[seriesId]`; out-of-range falls back to the current value. Same seed + state → same observation.
- **SPEC-FOG-2** `[testable]` Fog noise is distributionally correct: for a series whose `content/engine/fog.json` entry has `noise_scale > 0`, at least 2,000 `observe()` draws over a single seeded RNG stream (fixed state and history) have a sample mean within 3 standard errors of the lagged truth and a sample standard deviation within ±10% of the configured `noise_scale`. The test reads the configured `noise_scale`/`lag_months` from the content file rather than hardcoding them.

## Committee
*`SPEC-COMM-1`/`SPEC-COMM-2` defined the original committee model; `SPEC-COMM-3` redesigned it around empirical FOMC dynamics after player feedback that the trichotomy produced 1500bp dot spreads instead of the historical ~150bp.*

- **SPEC-COMM-1** `[testable]` A committee schema (`schemas/committee.schema.json`) and content (`content/committees/1979.json`) define members with stable loc-key names and per-member coefficient fields. `name` is constrained to localization-key shape `^[a-z][a-z0-9_.]+$`. `npm run validate` accepts `content/committees/1979.json`. No engine changes in this SPEC.
- **SPEC-COMM-2** `[testable]` A pure `vote(committee, proposedRate, state, params): { decided, dissents }` simulates an FOMC vote and counts members where `|preferred - proposedRate| > member.compromise_band` (threshold is per-member — see SPEC-COMM-4). `decided` is `proposedRate` for slice 1 (the committee has no override power yet). `params` is required — callers resolve via `loadCommitteeParams()`. Missing or non-finite `state.vars.inflation`, `state.vars.unemployment`, or `state.vars.policy_rate` throws `VoteMissingVarError`; non-finite `proposedRate` throws a plain `Error` (caller bug, not a content authoring error). No silent default-to-zero in any path. The dissent count is surfaced in the `FomcVote` and the committee briefing UI, but per issue #33 (SPEC-CRED-1) dissents no longer feed `applyMeetingOutcome` / credibility.
- **SPEC-COMM-3** `[testable]` Per-member preferred rate is a Taylor-rule reaction function with **continuous per-member coefficients** (`inflation_coef`, `output_coef`, `inertia`), anchored at the committee-wide `neutral_rate` and smoothed against the lagged policy rate. The formula is `inertia * laggedRate + (1 - inertia) * (neutral_rate + inflation_coef * (inflation - target_inflation) - output_coef * (unemployment - target_unemployment))`. The 1979 committee has **12 voting members** (7 governors + NY Fed permanent + 4 rotating regional presidents), matching the historical FOMC. Empirical anchors live in `docs/research/2026-06-02-fomc-empirical-anchors.md`. The dropped `hawkish_inflation_weight` / `dovish_unemployment_weight` / `neutral_blend` / per-member `lean` enum are removed entirely; an additional `previewVote` helper returns the per-member preferred rates a UI needs to surface the briefing.
- **SPEC-COMM-4** `[testable]` Each committee member carries their own `compromise_band` (the range around their preferred rate they will assent to); `vote()` and `previewVote()` judge dissent per-member using that band. The global `dissent_tolerance` field is removed from `CommitteeParams`. Given two members with different band widths, a proposal at a distance inside the wide-band member's tolerance but outside the narrow-band member's produces exactly one dissent. Given all members have the same band width equal to the old global default, behavior is unchanged.
- **SPEC-COMM-5** `[testable]` A trait system for committee members: a continuous `conviction` dial per member (in [0, 1]) that narrows the effective compromise band (`effective_band = compromise_band * (1 - conviction * conviction_band_factor)`, where `conviction_band_factor` is schema-governed in `content/engine/committee.json`); plus a named, content-defined trait catalog (`schemas/traits.schema.json`, `content/traits/`) whose entries declare always-on effects (`preferred_rate_shift`, `band_modifier`) and signal-reactive hooks (dormant until the referenced series exists). Given a high-conviction member, their effective band is narrower than a low-conviction member with the same `compromise_band`. Given a member tagged with a hawkish-lean trait, their preferred rate shifts up by the trait's declared amount. `npm run validate` accepts a well-formed trait catalog and rejects one missing a required field.
- **SPEC-COMM-6** `[testable]` Between FOMC meetings each member's preferred-rate (stance) drifts toward their Taylor-rule prescription from incoming macro data. A pure `applyIntermeetingDrift(state, committee, params): GameState` evolves each member's stance one month: `new_stance = inertia * prev_stance + (1 - inertia) * taylor_target`, where `taylor_target = neutral_rate + inflation_coef * gapInflation - output_coef * gapUnemployment`. The previous stance is read from `state.vars["stance.<memberId>"]`; on first call (key absent) it falls back to `state.vars.policy_rate`. Stances are written back to `state.vars["stance.<memberId>"]`. `previewVote` uses the stored stance as the lagged-rate anchor if present, so meeting stances reflect the intermeeting period rather than recomputing cold from `policy_rate`. `applyIntermeetingDrift` is called each month in `Session.advance()`. Given two members identical except for `inflation_coef`, a one-month drift with rising inflation moves the higher-`inflation_coef` member's stance more. Given a state near steady state, drift produces negligible change. Pure: no `Math.random` / `Date.now` (SPEC-SIM-1).
- **SPEC-COMM-7** `[testable]` Chair capital: a per-meeting persuasion budget computed from the Chair's credibility (`base_capital + floor(credibility_weight * credibility)`), spendable to widen a chosen member's compromise band for that meeting only — ephemeral, not stored in state. `computeChairCapital(credibility, params): number` returns the integer budget. `computeEffectiveBands(capitalSpend, committee, params)` maps a spend allocation to per-member widened bands; if any entry exceeds `max_spend_per_member`, it throws rather than silently capping (hard persuasion budget — see `Session._assertWithinBudget`). `previewVote` and `vote` accept an optional `effectiveBands` map so the player can see the effect before committing. Given a higher credibility, the budget is larger. Given capital is spent on member M, M assents to a proposal they would otherwise have dissented on; given the next call without spend, M's band reverts to the unspent width. Params (`base_capital`, `credibility_weight`, `band_widen_per_unit`, `max_spend_per_member`) live in `content/engine/chair-capital.json` (schema-governed).
- **SPEC-COMM-8** `[testable]` Committee dot-plot spread realism: the per-member Taylor-rule coefficients are calibrated so the spread of preferred rates reads like the real SEP dot plot — meaningful hawk/dove disagreement, but bounded — rather than the slice-1 trichotomy or an unrealistically wide fan. For every **playable** shipped scenario (`loadScenarioCatalog().filter(s => s.playable)`), the spread `max(preferred) − min(preferred)` over `previewVote` at the scenario's starting `policy_rate` is at most `150 bp` (0.015), and is strictly positive (the committee is not unanimous). Enforced by `test/committee-spread.test.ts`, which also asserts the hawk-leaning members sit above the dove-leaning ones (ordering preserved through calibration).

## Engine params
- **SPEC-PARAMS-1** `[testable]` Engine-tunable params are split into per-section content files (`content/engine/tick.json`, `content/engine/fog.json`, `content/engine/credibility.json`, `content/engine/committee.json`) each validated by a corresponding per-section schema (`schemas/tick.schema.json`, `schemas/fog.schema.json`, `schemas/credibility.schema.json`, `schemas/committee-params.schema.json`). The umbrella `schemas/engine-params.schema.json` and `content/engine/params.json` are deleted. A new `loadValidatedFile(schemaPath, filePath)` helper in `src/content/loader.ts` validates a single JSON file and caches the compiled AJV `ValidateFunction` in a module-level `Map` keyed by `schemaPath` so the same schema is compiled at most once per process. Callsites updated: `src/engine/clock.ts`, `src/engine/fog.ts`, `src/engine/credibility.ts`, `src/engine/fomc.ts`, and `tools/validate-content.ts`.

## Engine session
- **SPEC-SESSION-0** `[testable]` A pure Session façade wraps tick + vote + applyMeetingOutcome with identity-stable getters and a subscribe protocol consumed by useSyncExternalStore in the future web UI. Skeleton in slice 2; the successor spec fully wires the macro dynamics. `Session.fromScenario(scenarioId, seed, committeeId)` and `Session.fromReplay(replayId, seed, committeeId)` are the two static factories; `committeeId` identifies the FOMC committee content file used by `proposeRate`. `advance(months)`, `proposeRate(rate)`, `reset()`, and `setForwardGuidanceStance(stance)` are the mutators. `get current(): GameStateSnapshot` and `get trajectory(): readonly GameStateSnapshot[]` return referentially-stable values that change reference only on mutation. `subscribe(listener)` returns an unsubscribe function; listeners fire synchronously after each mutation but never on getter reads.
- **SPEC-SESSION-1** `[testable]` `Session.proposeRate()` is gated to scheduled FOMC meeting months defined in `content/engine/meeting-schedule.json` (schema-governed). `Session.isMeetingMonth(date?: string): boolean` returns true iff the given date's month falls in the schedule. Calling `proposeRate()` outside a meeting month throws `NotMeetingMonthError` (with `public readonly date: string`).
- **SPEC-GUIDE-2** `[testable]` Proposing a rate inconsistent with the active forward-guidance stance surprises markets and costs credibility. A pure `marketsSurprised(stance, currentRate, decidedRate, tolerance): boolean` returns true when the decided rate's move contradicts the guided direction: a **hawkish** stance is surprised by an easing (`decided - current < -tolerance`), a **dovish** stance by a tightening (`decided - current > tolerance`), and a **neutral** stance by any move beyond the band (`|decided - current| > tolerance`); a move within `tolerance` of the current rate never surprises. `Session.proposeRate` computes this from the stored stance and the pre-meeting policy rate and feeds it to `applyMeetingOutcome`, activating the SPEC-CRED-1 `-5` surprise lever that was previously pinned `false`. `surprise_tolerance` lives in `content/engine/guidance.json` (schema-governed).
- **SPEC-GUIDE-1** `[testable]` `Session.setForwardGuidanceStance(stance)` scales the expectations re-anchoring pull. Inside `Session.advance()`, the `expectations_anchor_pull` passed to `applyMacroDynamics` is multiplied by `stance_multiplier` — `hawkish_multiplier`, `neutral_multiplier`, or `dovish_multiplier` from `content/engine/guidance.json` (schema-governed) — so hawkish guidance re-anchors expectations toward `target_inflation` faster and dovish slower. The stance is stored on the Session, not in `state.vars`.

## Calibration
- **SPEC-CAL-1** `[testable]` Real FRED data for 1979-08 through 1986-12 is committed as `content/calibration/fred_1979_1986.json` and a calibration harness `tools/calibrate.ts` runs the canned 1979 chair-tightening replay through the engine and emits a CSV comparing engine output to FRED. A `npm run calibrate` script produces the comparison. The harness now drives the full engine (`Session.advance` runs the real-rate dynamics) and reports policy_rate, inflation, and unemployment RMSE; the SPEC-CAL-1 test asserts the FRED data loads correctly and the harness runs deterministically, while the asserted convergence tolerances live in SPEC-CAL-2.
- **SPEC-CAL-2** `[testable]` Driving `Session.fromReplay("replay.1979_chair_tightening", …)` forward month-by-month across the full 1979-08…1986-12 window (so the real-rate dynamics, expectations, and mission-tied credibility all run) reproduces the FRED baseline within tolerance: the root-mean-square error of the engine trajectory vs FRED, aligned month-for-month, is `< 0.025` (2.5pp) on `inflation` and `< 0.020` (2.0pp) on `unemployment`. The disinflation arc is asserted directionally too (inflation ends well below its 1979 level; unemployment peaks in recession then partly recovers; credibility ends above its start). This pins the realistic-dynamics calibration so macro-model regressions surface as a failing test; two runs are identical (determinism). The 1979-80 inflation peak (second oil shock) is a supply shock left to content events, not the demand-side core, so the tolerance is deliberately loose.
- **SPEC-CAL-3** `[testable]` Calibration thresholds are content-governed, not hardcoded. A new content type `schemas/calibration-thresholds.schema.json` defines `CalibrationThresholds` (`inflation_rmse_max`, `unemployment_rmse_max`, `policy_rate_rmse_max` — all positive numbers). One committed instance: `content/engine/calibration-thresholds.json`. `tools/validate-content.ts` registers it. A `loadCalibrationThresholds()` function (with module-level cache) reads and validates the file. `tools/calibrate.ts` is extended to call `loadCalibrationThresholds()` and, after computing RMSE values, emit a `PASS`/`FAIL` verdict line for each metric to stderr (format: `inflation    RMSE: 0.0123 PASS (< 0.0250)`). The harness exits with code 0 even when a metric fails — this is a "soft gate" that warns without blocking. A test in `test/calibration-thresholds.test.ts` asserts: `loadCalibrationThresholds()` loads successfully and returns finite positive values; the schema rejects a file with a negative threshold.

## Web UI

- **SPEC-WEB-1** `[testable]` A Vite 5 + React 18.3 + TypeScript 5 scaffolding is set up in `web/` as a self-contained subdirectory with its own `package.json` (not npm workspaces). Root `package.json` gains `web:install`, `web:dev`, `web:build` script proxies. Root `npm run check` is extended to include `(cd web && tsc --noEmit)`. `npm run web:build` produces `web/dist/index.html`. `web/dist/` and `web/node_modules/` are gitignored.
- **SPEC-WEB-2** `[testable]` A `<Dashboard />` React component renders the engine's `Session` state — current date, policy_rate, inflation, unemployment, credibility, expectations_anchor, months_below_anchor, and trajectory length — with controls to advance 1/3/12 months and reset. A `useSession(scenarioId, seed, committeeId)` hook backed by `useSyncExternalStore` subscribes to the Session and re-renders the dashboard on each mutation. The engine's `src/content/loader.ts` gains a content registry so browser code can pre-register every JSON file at module load time (`web/src/engine-content.ts`) and the engine's `readFileSync`/`readdirSync` paths are never invoked in the browser bundle. Stub modules at `web/src/node-stubs.ts` resolve `node:fs` / `node:path` so Vite bundles cleanly. AC: vitest-jsdom test asserts the dashboard renders the 1979 scenario's initial state and that clicking "Advance 1 month" advances the displayed date by one month.
- **SPEC-WEB-4** `[testable]` A `<MeetingPanel session={Session} />` component surfaces the FOMC meeting flow. In a meeting month the "Propose rate" button is enabled and submitting calls `session.proposeRate(rate)`, displaying the decided rate, dissent count, and credibility delta. Outside a meeting month the button is disabled and the panel labels the month as non-meeting. A committee briefing table shows each member's preferred rate and whether they would dissent, sourced from `session.committeeBriefing(proposedRate)`. `committeeBriefing` is a pure Session method that calls `previewVote` without mutating any state; it also exposes `inflationTarget` and `unemploymentTarget` so gap labels are dynamically bound to content. All player-facing strings in the component use `t()` from `web/src/loc.ts`. AC: test asserts "Propose rate" is disabled outside a meeting month and enabled inside; `committeeBriefing` unit test asserts previews + gap fields are finite and dissent count matches `proposeRate`.
- **SPEC-WEB-5** `[testable]` The dashboard gains an "Advance to next meeting" button (calls a helper that finds the nearest future FOMC meeting month without advancing state first, then advances exactly that many months) and hawkish/neutral/dovish stance buttons (call `session.setForwardGuidanceStance(stance)`). If no meeting is found within 12 months, the button shows an error without mutating game state. All button labels from localization. AC: test asserts advance-to-next-meeting from a non-meeting month correctly lands on the next meeting month; stance buttons do not throw.
- **SPEC-WEB-6** `[testable]` An in-meeting persuasion view (`<PersuasionView>`) renders: a dot plot of members' preferred rates on a number line (one SVG dot per member, colored by dissent status, with a proposed-rate marker) and a live dissent count that updates with the proposed rate; a scenario-briefing panel surfacing the raise/hold/lower forecast cards from SPEC-BRIEF-1 content when a `briefingId` is supplied; and a Chair-capital spend control that degrades gracefully (shows "—") when no capital props are supplied (the wired control is SPEC-WEB-8). All player-facing strings use `t()`. The `buildDotPlotData(previews, proposed)` helper is exported for pure-unit testing. AC: test asserts one dot per member, dissent count matches `wouldDissent` flags, three scenario cards appear with a valid briefingId, graceful no-throw with an unknown briefingId, and the capital control degrades to "—" when capital props are absent.
- **SPEC-WEB-3** `[testable]` A `<ChartsPanel trajectory={...} />` component renders 4 time-series (inflation, unemployment, policy_rate, credibility) via `@observablehq/plot`. Each series gets a fog overlay: a translucent band of height `2 × noise_scale` (from `content/engine/fog.json`) centred on each data point. AC: vitest-jsdom test asserts the chart-data arrays passed to the component contain the correct values for each series and that the fog-band half-width equals each series' `noise_scale`.
- **SPEC-WEB-7** `[testable]` A `<DoctrinePanel session={Session} current={snapshot} />` component, mounted in the Dashboard, exposes doctrine management: it lists every entry from `loadDoctrineCatalog()` with localized name and description, marks adopted doctrines (adopted state read from the snapshot's flags via `doctrineFlagKey`), and offers an Adopt button (calls `session.adoptDoctrine(id)`) for unadopted entries. Abandoning is two-step: an Abandon button reveals the doctrine's `flip_flop_cost` plus a Confirm button (calls `session.abandonDoctrine(id)`) and a Cancel button — the cost is always visible before the abandon is committed. Engine errors surface as an inline message without crashing the panel. All player-facing strings use `t()` from `web/src/loc.ts`. AC: vitest-jsdom test asserts all three doctrines render; adopting Gradualism raises displayed credibility by its standing effect; the abandon flow shows the flip-flop cost before confirmation; confirming deducts the standing effect plus flip-flop cost from displayed credibility; and the adopted badge toggles accordingly.
- **SPEC-WEB-8** `[testable]` The Chair-capital spend control is wired to SPEC-COMM-7. `MeetingPanel` owns a per-member `capitalSpend` map (cleared when the displayed date changes and after a successful vote), reads the meeting budget from `Session.chairCapital()`, and threads the map into both `session.committeeBriefing(rate, capitalSpend)` — so the dot plot and member table reflect widened effective bands live — and `session.proposeRate(rate, capitalSpend)`. `PersuasionView` renders the budget, the allocated total, and one spend input per member; `MeetingPanel` clamps each spend client-side to `[0, max_spend_per_member]` (from `content/engine/chair-capital.json`) and to the remaining budget, so engine overdraw errors are unreachable through the UI. All player-facing strings use `t()`. AC: tests assert the control shows the numeric budget equal to `chair-capital` params applied to current credibility; changing a member's spend re-invokes `committeeBriefing` with that spend map; proposing passes the same map to `proposeRate` and clears it afterwards; and spend values clamp to the per-member max and remaining budget.
- **SPEC-WEB-9** `[testable]` Player-visible inflation and unemployment readouts use fogged observations, not true vars. `Session.observed(seriesId, index?)` returns the SPEC-FOG-1 observation of a trajectory point using a derived RNG seeded by FNV-1a (`fnv1a32` in `src/engine/rng.ts`) over `(session seed, snapshot date, seriesId)`: deterministic (identical inputs yield identical observations; a historical point's observation never changes as play continues) and side-effect-free (the session RNG is never consumed, so reading observations cannot perturb a subsequent `advance()`). `Session.mandateOnTarget()` exposes the SPEC-MANDATE-1 evaluator on the current state. The Dashboard's inflation/unemployment stat tiles and the ChartsPanel inflation/unemployment series render observed values (fog bands therefore center on the observation), and the Dashboard adds long_rate and output_gap stats plus a mandate on-target indicator. AC: node tests assert observation determinism, twin-session trajectory non-perturbation, historical-observation stability, content-driven pass-through (a zero-noise/zero-lag series observes exactly the true var) and divergence (a noisy series differs from truth within a 6-sigma bound of its noise_scale), distinct noise draws across series, mandateOnTarget equivalence to `onTarget`, and out-of-range/unknown-series errors; a web test asserts the tiles show the twin-session observed values and the mandate chip reflects mandateOnTarget().
- **SPEC-WEB-10** `[testable]` A start screen precedes the game. `schemas/scenario.schema.json` gains optional `playable` (boolean) and `briefing` (briefing content id) fields; `loadScenarioCatalog()` in `src/content/scenarios.ts` returns the full catalog and the start screen offers exactly the scenarios with `playable === true` — `scen.recovery_test` is a test fixture and must never be offered. The picker takes an optional seed (default 42) and boots `<Dashboard scenarioId seed briefingId varDeltas?>` (the Dashboard no longer hardcodes a scenario; the meeting briefing id comes from the scenario's `briefing` field). `Session.fromScenario(scenarioId, seed, committeeId, opts?)` accepts optional `varDeltas` — additive adjustments applied to the scenario's starting vars; an unknown var name or non-finite delta throws. The confirmation-hearing path renders `hearing.confirmation`'s questions; submitting resolves via `resolveHearing` (SPEC-HEAR-1) to the winning scenario plus accumulated `state_modifiers`, which boot the session as `varDeltas`; an incomplete submission shows an error without booting. AC: web tests assert the picker lists exactly the playable scenarios (recovery_test absent); picking one boots that scenario (id and date from content); the seed input reaches the session (twin-session fogged-observation equality at a non-default seed); the hearing path boots the `resolveHearing`-computed scenario with a starting var equal to scenario value plus the content-declared deltas; node tests assert the catalog/playable filter, the briefing id shape on playable scenarios, and `varDeltas` semantics (additive, unknown-var and non-finite throws, omitted opts unchanged).

## Name generation
- **SPEC-NAME-1** `[testable]` A deterministic, seeded NPC name generator usable for every named character (committee members, division heads, future actors). Name parts are content-governed: `content/names/pools.json` (schema `schemas/names.schema.json`) supplies `honorifics`, `given_names`, and `surnames` string arrays. A pure `generateName(rng, pools): { honorific?: string; given: string; surname: string; full: string }` in `src/engine/names.ts` draws from the pools using only the caller's seeded `rng` (SPEC-SIM-1 — never `Math.random`/`Date.now`); a derived helper `nameForId(seed, npcId, pools): ...` seeds a stream via `fnv1a32(`${seed}|${npcId}`)` so the same `(seed, npcId)` always yields the same name and distinct ids yield independent draws. `Session.npcName(npcId)` returns the full generated name for an id using the session seed, so committee/UI code no longer reads hardcoded localization name values. Generated full names never collide with the real-person blocklist used by SPEC-CONTENT-3: a test draws at least 500 names across distinct ids and asserts every one passes the blocklist, and that the pool entries themselves contain no blocklisted token. `npm run validate` accepts a well-formed pools file and rejects one missing a required array.

## Institution & resources (engine)
- **SPEC-INST-1** `[testable]` Two institutional resources are tracked as state vars: `operating_budget` (the self-funding Fed; grows over time) and `political_capital` (Congress-facing; drifts toward a baseline). A pure `applyInstitutionDynamics(state, params): GameState` in `src/engine/institution.ts` reads each var (defaulting to `params.initial_operating_budget` / `params.initial_political_capital` when absent, like SPEC-PROD-1's productivity default so existing scenarios are unaffected), applies `operating_budget *= (1 + budget_monthly_growth)` and `political_capital += political_capital_recovery * (political_capital_baseline - political_capital)`, and returns a new state without mutating the input. Params live in `content/engine/institution.json` (schema `schemas/institution.schema.json`). `Session.advance()` calls it each month after `applyMacroDynamics`. Tests assert: monotone budget growth over N months; political capital mean-reverts toward baseline; defaults applied when vars absent; purity; `Session.advance(12)` yields finite `operating_budget`/`political_capital`. Session exposes `operatingBudget()` and `politicalCapital()` getters.
- **SPEC-INST-2** `[testable]` A division-staffing layer lets the Chair build the institution. A division catalog is content: `content/divisions/*.json` (schema `schemas/division.schema.json`), each with `id`, loc-key `name`/`desc`, a non-negative `hire_cost` (in political capital), and a positive `investment` contribution. A pure, deterministic `generateCandidates(divisionId, seed, pools, params): Candidate[]` in `src/engine/institution.ts` returns a fixed-size slate (`params.candidate_slate_size`) of candidates, each with a generated name (SPEC-NAME-1, seeded by `fnv1a32(`${seed}|${divisionId}|${index}`)`), a `competence` in [0,1], and a `lean` drawn deterministically from the seeded stream — identical inputs reproduce the slate exactly. A pure `hireStaff(state, division, candidate): GameState` records the hire (`state.flags[staffedFlagKey(divisionId)] = true`, `state.vars["staff." + divisionId + ".competence"] = candidate.competence`), deducts `hire_cost` from `political_capital` (throwing `InsufficientCapitalError` if the balance would go negative, and `DivisionAlreadyStaffedError` if already staffed), and never mutates the input. `institutionInvestment(state, catalog): number` sums `division.investment * staffCompetence` over staffed divisions — this is the abstract `investment` value (exposed via `Session.institutionInvestment()`) intended to drive SPEC-BRIEF-2 forecast quality in a follow-up. `npm run validate` accepts a well-formed division file and rejects one missing a required field. Session exposes `divisionCatalog()`, `candidatesFor(divisionId)`, `isStaffed(divisionId)`, `institutionInvestment()`, and a `hire(divisionId, candidateIndex)` mutator that fires listeners. No real person names in any division content (SPEC-CONTENT-3).

## Director skills, division functions & institutional culture
- **SPEC-STAFF-1** `[testable]` Directors are skill vectors and divisions demand skills, so matching a person to a post matters. `generateCandidates` (SPEC-INST-2) additionally draws a `skills` object — `{ forecasting, markets, supervision, communication, crisis }`, each in [0,1] — deterministically from the candidate's seeded stream (`Candidate` gains `skills`). Each division content file gains `skill_weights` (a map over the same five skills; schema requires all five, each ≥ 0). A pure `directorEffectiveness(skills, weights): number` returns the weighted dot product `Σ weights[s] * skills[s]` normalized by `Σ weights[s]` → [0,1]. `hireStaff` stores the hired director's effectiveness for that division in `state.vars["staff.<id>.eff"]` (alongside the existing competence). Tests: skills are deterministic per `(seed, divisionId, index)`; `directorEffectiveness` math + normalization; the same skills score differently against two divisions' weights; schema accepts well-formed `skill_weights` and rejects a missing skill; `hireStaff` records `staff.<id>.eff`. Pure (SPEC-SIM-1).
- **SPEC-STAFF-2** `[testable]` Each director carries a **hidden disposition** — a continuous hawkish(+)/dovish(−) tilt in [-1,1], drawn deterministically from the candidate's seeded stream, distinct from (and uncorrelated with) the visible `lean`, and **not surfaced on the candidate card** (the player infers it over time from how the division behaves). `Candidate` gains `disposition`; `hireStaff` stores `state.vars["staff.<id>.disposition"]`. The disposition *slightly* colors that division's channel output via a small content `disposition_influence`, manifesting per channel: a hawkish **Supervision** head supervises harder (more `fragilityMitigation`); a hawkish **Financial Stability** head is more vigilant (more `fragilityVisibility`); a hawkish **Research** head's forecasts skew slightly toward higher inflation (a `forecastBias` term in `DivisionEffects`); and any director's disposition feeds `culture.policy_lean` alongside their `lean` (so a hawkish **Monetary Affairs** head tilts the institution hawkish). Dovish dispositions reverse each. The adjustment is bounded by `disposition_influence` so it never dominates the skill-based effectiveness. `divisionEffects` applies the per-channel disposition adjustment (reading `staff.<id>.disposition`); `applyCultureDrift` blends disposition into `culture.policy_lean`; SPEC-WEB-14's candidate card must not display the disposition value. Tests: disposition is deterministic per `(seed, divisionId, index)` and not equal to the lean encoding; a hawkish vs. dovish Supervision head with identical skills yield different `fragilityMitigation`; the per-channel adjustment magnitude is bounded by `disposition_influence`; purity (SPEC-SIM-1).
- **SPEC-DIV-1** `[testable]` Each staffed division drives a real channel. A pure `divisionEffects(state, catalog, params): DivisionEffects` aggregates, over staffed divisions, each division's `channel` (content field) scaled by `state.vars["staff.<id>.eff"]` and a content `effect_strength`. `DivisionEffects` is `{ fogFactor, transmission, fragilityVisibility, fragilityMitigation, crisisSeverityReduction, externalShockDamp }` (all finite; `fogFactor`/`externalShockDamp` are multipliers in (0,1], the rest are non-negative contributions). A division staffed by a poor-fit director (effectiveness below `competence_floor`) contributes a reduced or slightly negative effect on its channel (mismatch underperforms). Unstaffed divisions contribute nothing. Params live in `content/engine/division-effects.json` (schema-governed). Tests: each channel strengthens with effectiveness; an unstaffed catalog yields the neutral identity (fogFactor=1, no mitigation); a below-floor fit underperforms a strong fit; purity.
- **SPEC-DIV-2** `[testable]` The division catalog covers the Board's staffable divisions, not a token five. `content/divisions/` ships ≥ 10 divisions (research, monetary_affairs, financial_stability, supervision, international, rbops, consumer_community, legal, coo, oig), each validating against `schemas/division.schema.json` with `skill_weights` + `channel`. An optional `unlocked_by` field (defaults to always-available) is schema-allowed for future tech-gating. `npm run validate` is green; no real person names appear (SPEC-CONTENT-3); `loadDivisionCatalog()` returns them all; every `division.<id>.name`/`.desc` loc key resolves in `content/localization/en.json`.
- **SPEC-CULTURE-1** `[testable]` The institution accretes a culture from the Chair's hires that lags and persists. A pure `applyCultureDrift(state, catalog, params): GameState` updates two vars via EWMA toward the current staffed cohort: `culture.policy_lean` ∈ [-1,1] (mean numeric `lean` of staffed directors, hawk=+1/centrist=0/dove=-1) and `culture.supervisory_rigor` ∈ [0,1] (effectiveness-weighted contribution of the supervision + financial_stability divisions). EWMA half-life (`culture_half_life_months`) and the rigor weights live in `content/engine/culture.json` (schema-governed). When no relevant division is staffed, culture drifts toward a content baseline, not undefined. Tests: culture moves toward a newly-staffed cohort over months but lags (one month ≠ full jump); replacing directors shifts it gradually; baseline pull when unstaffed; purity (SPEC-SIM-1).

## Banking stability & the financial cycle
- **SPEC-FRAG-1** `[testable]` A `bank_fragility` var ∈ [0,1] models systemic banking risk (read as a CAMELS-like composite; 0 = pristine, 1 = crisis-prone). A pure `applyFragilityDynamics(state, inputs, params): GameState` computes `fragility += accumulation − mitigation`, clamped to [0,1], where `inputs = { realGap, easingSpeed, supervisoryRigor, fragilityMitigation }`: `accumulation = base + loose_policy_weight * max(0, −realGap) + easing_weight * max(0, easingSpeed) + lax_weight * (1 − supervisoryRigor)` (loose policy and sustained easing breed fragility; a lax supervisory culture compounds it) and `mitigation = supervisory_decay * fragilityMitigation + natural_decay`. Params live in `content/engine/fragility.json` (schema-governed). Defaults `bank_fragility` to a content `initial_fragility` when absent. Pure. Tests: loose policy (negative realGap) raises fragility; strong mitigation/rigor lowers it; clamps at [0,1]; defaults when absent; purity.
- **SPEC-CRISIS-1** `[testable]` Banking crises are endogenous, not scripted. A pure `crisisProbability(fragility, params): number` returns `clamp(crisis_base + crisis_slope * max(0, fragility − crisis_threshold), 0, 1)`. A pure `applyFinancialCrisis(state, severityReduction, params, rng): GameState` injects a credit/demand shock — unemployment up by `severity * (1 − severityReduction)`, inflation down, credibility down, `output_gap` down — and resets `bank_fragility` to `post_crisis_fragility` (the cleansing — a crisis purges accumulated risk), drawing any stochastic magnitude from the caller's seeded `rng` (SPEC-SIM-1, never `Math.random`). Params live in `content/engine/crisis.json` (schema-governed). `Session.advance()` draws a seeded Bernoulli with `crisisProbability` each month (after fragility), applies a crisis on a hit, and sets a `crisis` flag for the UI; a content cooldown prevents back-to-back crises. Tests: probability is monotonically increasing above threshold and 0 below; a crisis raises unemployment + lowers inflation/credibility + cuts fragility; severityReduction softens the hit; determinism (same seed → same crisis path); purity; two Session runs with the same seed are identical.

## Federal Reserve finances
- **SPEC-FED-1** `[testable]` The Fed's balance sheet and income are modeled realistically enough to produce the post-2022 "deferred asset." Vars: `balance_sheet` (SOMA size, normalized), `portfolio_yield` (book yield), `net_income`, `deferred_asset`. A pure `applyFedFinances(state, params): GameState` (run each month after `applyTermStructure`, which sets `long_rate`): `portfolio_yield` is an EWMA toward `long_rate` with a long content half-life (`portfolio_half_life_months`), cold-starting to `policy_rate`; `net_income = (portfolio_yield − policy_rate) * balance_sheet` (carry — negative when the policy rate paid on liabilities exceeds the legacy portfolio's book yield); if `net_income < 0` then `deferred_asset += −net_income` and no remittance, else the surplus first pays `deferred_asset` down toward 0 and the remainder lifts `operating_budget`. All vars default from content (`initial_*`) when absent. Params live in `content/engine/fed-finances.json` (schema-governed). Pure. Tests: hiking `policy_rate` above `portfolio_yield` on a large balance sheet drives `net_income < 0` and grows `deferred_asset`; a positive carry pays the deferred asset down before lifting the budget; `portfolio_yield` EWMA toward `long_rate`; cold-start to `policy_rate`; purity; `Session.advance(12)` yields finite finances vars.
- **SPEC-CONGRESS-1** `[testable]` Fed losses draw political scrutiny and pressure independence. An `independence` var ∈ [0,100] (the fiscal-dominance axis). A pure `applyCongressionalPressure(state, params): GameState`: when `deferred_asset` exceeds `inquiry_threshold`, deduct `political_capital_drain` from `political_capital` (clamped ≥ 0) and `independence_drain` from `independence` (clamped [0,100]), and set `flags["pending_inquiry.deferred_asset"] = true`; below the threshold it is a no-op and clears the flag once `deferred_asset` returns to 0. `independence` defaults from content when absent. Params live in `content/engine/congress.json` (schema-governed). Pure. `Session.advance()` runs it each month after `applyFedFinances`. Session exposes `independence()`, `netIncome()`, `deferredAsset()`, `balanceSheet()`, `bankFragility()`, `divisionEffects()`, `culture()`. Tests: above-threshold deferred asset drains capital + independence and sets the flag; below-threshold no-op; clamps; flag clears on recovery; purity.

## Staffing economy & the living organization
- **SPEC-STAFF-3** `[testable]` Staffing is the Chair's job, not a scarce favor: hiring is funded by the **operating budget** (the self-funding Fed), never political capital, and the starting budget covers a full roster so the Chair can staff every division immediately. `hireStaff` deducts `division.hire_cost` from `operating_budget` (default from content when absent) and throws `InsufficientBudgetError` only when the budget cannot cover it (it can at game start); it no longer touches `political_capital`. A pure `fireStaff(state, division): GameState` clears the appointment (`staffed.<id>` flag false; removes `staff.<id>.*` vars) so the Chair can dismiss and replace a director — after firing, `candidatesFor` offers a fresh slate and the division can be re-staffed. Pure (SPEC-SIM-1). The director's hawkish/dovish orientation (visible `lean` and hidden `disposition`) is **never surfaced in the UI** — the player infers it from behavior (enforced by SPEC-WEB-15). Session gains `fire(divisionId)` (fires listeners). Tests: hire deducts from `operating_budget` not `political_capital`; the starting budget affords the entire catalog; `InsufficientBudgetError` when budget is short; `fireStaff` clears the appointment and a re-hire succeeds; purity. `InsufficientCapitalError` is retired from the hire path.
- **SPEC-COMM-9** `[testable]` The Chair's committee persuasion budget grows with a consensus track record, not just credibility. A `consensus_capital` var (≥ 0, default 0) rises by `consensus_gain` after a meeting decided with **zero dissents** and falls by `consensus_penalty` after a meeting with more than `dissent_penalty_threshold` dissents (clamped ≥ 0); `Session.proposeRate` updates it from the vote outcome. `computeChairCapital(credibility, params, consensusCapital = 0)` returns `base_capital + floor(credibility_weight * credibility) + floor(consensus_weight * consensusCapital)` — the third term is new and defaults to 0 so existing callers are unaffected. Params (`consensus_gain`, `consensus_penalty`, `consensus_weight`, `dissent_penalty_threshold`) live in `content/engine/chair-capital.json`. `Session.chairCapital()` passes the stored `consensus_capital`. Tests: a zero-dissent meeting raises `consensus_capital` and thus the next meeting's budget; a high-dissent meeting lowers it (clamped ≥ 0); `computeChairCapital` with a positive consensus term exceeds the same call with 0; backward compatibility (omitted arg ⇒ old value).
- **SPEC-EVENT-1** `[testable]` A pure escalation engine surfaces decisions at the Chair so the organization feels alive. `loadEventCatalog()` (`src/content/events.ts`) validates `content/events/*.json` against `schemas/event.schema.json`. A pure `eligibleEvents(state, catalog, firedOnce: ReadonlySet<string>): Event[]` returns events whose `trigger` condition holds (via the SPEC-COND-1 evaluator) and which are not a spent `fires_once`. A pure `eventFireProbability(event, state, daysPerTick): number` converts the Paradox-style `mean_time_to_happen` (base_days × applicable modifier factors) into a per-month fire probability in [0,1] (an event with no MTTH is always-eligible → probability 1). `Session.advance()` draws, for each eligible event, a **derived** seeded RNG keyed by `(seed, date, eventId)` (so it never perturbs the supply-shock/crisis streams, SPEC-SIM-1) and appends fired events to a pending-escalation queue exposed by `Session.escalations(): readonly PendingEscalation[]`. Determinism: identical seed+inputs ⇒ identical escalations. Tests: eligibility respects triggers + `fires_once`; probability is 1 for no-MTTH and within (0,1) otherwise and rises with shorter effective days; two Session runs with the same seed surface identical escalations; purity.
- **SPEC-EVENT-2** `[testable]` Resolving an escalation applies the chosen option's effects and clears it. `Session.resolveEscalation(eventId, optionId)` finds the event + option, applies each effect through the pure SPEC-COND-2 effect engine (`add`/`sub`/`mul`/`set` on vars, `set_flag`, `trigger_event`), records the event in the fired-once set when `fires_once`, removes it from the pending queue, rebuilds caches, and fires listeners. Unknown event/option ids throw. A `trigger_event` effect enqueues the referenced event as a new escalation. Tests: resolving applies the option's var/flag effects to the snapshot; a `fires_once` event does not re-fire after resolution; an unknown id throws; resolving removes exactly that escalation from the queue.
- **SPEC-INST-5** `[testable]` The talent market turns over — candidate slates are not frozen forever. `generateCandidates(divisionId, seed, pools, params, refreshIndex = 0)` folds `refreshIndex` into each candidate's seed (`fnv1a32(`${seed}|${divisionId}|${refreshIndex}|${index}`)`), so a different index yields a different slate while a fixed index stays deterministic. `content/engine/institution.json` gains `candidate_refresh_months`. `Session.candidatesFor(divisionId)` computes `refreshIndex = dismissals + floor(monthsElapsed / candidate_refresh_months)`, where `dismissals` is the `refresh.<id>` var bumped by `Session.fire(divisionId)`. Effect: the slate refreshes immediately after a dismissal and periodically as time passes. Tests: a fixed `refreshIndex` is deterministic; two different indices give different slates; after `fire()` + re-querying, `candidatesFor` returns a different slate; `npm run validate` accepts the new param.
- **SPEC-FEED-1** `[testable]` The Chair keeps an activity ledger so decisions and events visibly matter. `Session.activityLog(): readonly ActivityEntry[]` returns dated entries (most recent first), each `{ date, titleKey, deltas: { var, delta }[] }`. Resolving an escalation logs an entry titled by the event, with the signed deltas of the feed-relevant vars (`credibility`, `political_capital`, `independence`, `bank_fragility`, `inflation`, `unemployment`, `operating_budget`, `output_gap`) that the chosen option moved; a financial crisis logs a `ui.feed.crisis` entry with its macro deltas. The `<ActivityFeed>` panel renders the log on The Desk (data-testid `activity-feed`, `activity-entry`), with an empty state and per-var localized labels. No display strings in engine code — entries carry loc keys; the UI formats deltas (pp for fractional series, plain otherwise). Tests: resolving an escalation with a credibility effect adds a log entry whose deltas include credibility; the log is empty at game start; `reset()` clears it; a web test asserts a rendered entry after resolving.

## Legacy & tenure
- **SPEC-LEGACY-1** `[testable]` The Chair serves a fixed renewable term and accrues a legacy. Params live in `content/engine/legacy.json` (schema `schemas/legacy.schema.json`): `term_length_months`, `reappointment_credibility_min`, and legacy weights (`legacy_credibility_weight`, `legacy_mandate_bonus`, `legacy_anchor_penalty`). Pure functions in `src/engine/legacy.ts`: `termProgress(monthsElapsed, params): { termLength, termsServed, monthsIntoTerm, monthsToReappointment, reappointmentDue }` (a reappointment is due at each term boundary); `evaluateReappointment(state, params): { reappointed: boolean; credibility: number; threshold: number }` (a referendum that succeeds iff `credibility >= reappointment_credibility_min`); and `computeLegacyScore(state, monthsElapsed, params): number` = `legacy_credibility_weight * credibility + legacy_mandate_bonus * (mandateOnTarget ? 1 : 0) * monthsElapsed - legacy_anchor_penalty * months_below_anchor` (mandate evaluated via SPEC-MANDATE-1). All pure (no `Math.random`/`Date.now`). `npm run validate` accepts the params file and rejects a negative `term_length_months`. Session exposes `termProgress()`, `reappointmentOutlook()`, and `legacyScore()`; `monthsElapsed` is `trajectory.length - 1`.

- **SPEC-WEB-11** `[testable]` The bare inspector dashboard is replaced by an immersive *Office of the Chair* game shell, `<AppShell>` in `web/src/AppShell.tsx`, mounted by `App` after the start screen. A persistent office header shows the Chair's generated name (`session.npcName("member.chair")`, SPEC-NAME-1), the current date, a term/tenure indicator (SPEC-LEGACY-1 `termProgress`), and credibility as a prominent gauge. The body is a tabbed navigation across rooms — **The Desk** (the SPEC-WEB-2/3/9 economy readouts, charts, time + guidance controls), **The Committee** (the SPEC-WEB-4/6/8 meeting + persuasion flow), **The Institution** (the SPEC-WEB-12 panel), and **Legacy** (the SPEC-WEB-13 panel + SPEC-WEB-7 doctrine panel). Every existing engine feature reachable in the old Dashboard remains reachable in the shell (nothing is orphaned). Visual styling is centralized in a shared theme module (`web/src/theme.ts`) — no per-component magic colors — establishing an institutional aesthetic; this is verified by review, but the structure is testable. All player-facing strings use `t()` from `web/src/loc.ts`. AC: a vitest-jsdom test asserts the shell renders the four tab controls, shows the Chair's generated name in the header, defaults to The Desk (date + a stat tile visible), and that activating each tab reveals its panel (meeting controls under The Committee, the institution heading under The Institution, the legacy heading under Legacy).
- **SPEC-WEB-12** `[testable]` An `<InstitutionPanel session={Session} current={snapshot} />` surfaces the institution-building layer. It shows `operatingBudget()` and `politicalCapital()` (SPEC-INST-1), and lists every division from `session.divisionCatalog()` (SPEC-INST-2) with localized name/desc, its `hire_cost`, and staffed/unstaffed status. For an unstaffed division it renders the candidate slate from `session.candidatesFor(divisionId)` — each candidate's generated name, competence, and lean — with a Hire button that calls `session.hire(divisionId, candidateIndex)`; a hire that would overdraw political capital surfaces the engine error inline without crashing the panel. A staffed division shows the hired head's competence (candidate names are shown in the slate before hiring) and the panel shows the running `institutionInvestment()`. All player-facing strings use `t()`. AC: a vitest-jsdom test asserts budget and political-capital readouts render, the catalog lists all divisions, hiring a division with sufficient capital marks it staffed and reduces the displayed political capital by `hire_cost`, and an unaffordable hire shows an inline error while leaving the division unstaffed.
- **SPEC-WEB-13** `[testable]` A `<LegacyPanel session={Session} />` surfaces tenure and legacy (SPEC-LEGACY-1). It renders the term clock (`termProgress`: terms served, months into the current term, months remaining until the next reappointment), the reappointment outlook (`reappointmentOutlook()`: whether current credibility clears `reappointment_credibility_min`, with the threshold shown), and the current `legacyScore()`. The reappointment indicator is visually distinct when the Chair would fail reappointment vs. pass. All player-facing strings use `t()`. AC: a vitest-jsdom test asserts the term clock, the reappointment outlook (pass/fail reflecting a twin session forced above/below the threshold), and the numeric legacy score all render.

- **SPEC-WEB-14** `[testable]` The institution-depth systems are surfaced and legible. Candidate cards in `InstitutionPanel` show each candidate's **skill vector** and the **fit** (effectiveness) computed for *that* division, so a poor match is visible before hiring (data-testid `candidate-fit-<divId>-<idx>`). The Institution tab shows what each staffed division is *doing* (its `divisionEffects` contribution), the institutional `culture` readout (policy lean + supervisory rigor), and the Fed-finances block (`balanceSheet`, `netIncome`, `deferredAsset`) with a visible warning state when a deferred asset is outstanding (data-testid `fed-finances`, `fed-deferred-asset`). The Desk tab adds a **financial-stability** readout — a `bankFragility` gauge (data-testid `stat-fragility`) — and an `independence` headline gauge beside credibility (data-testid `shell-independence`); an active financial crisis surfaces as a banner (data-testid `crisis-banner`). All player-facing strings use `t()`. AC: vitest-jsdom tests assert the fragility + independence readouts render, a candidate card shows its fit, the deferred-asset warning appears when a twin session is forced into a deferred asset, and the crisis banner appears when a crisis flag is set.

- **SPEC-WEB-15** `[testable]` The shell surfaces the living organization and the staffing/committee fixes. (1) **Escalations in-tray**: a prominent panel (on The Desk, above the economy) lists `session.escalations()` — each with its localized title/desc and a button per option that calls `session.resolveEscalation(eventId, optionId)`; resolving removes it (data-testid `escalation-<eventId>` and `escalation-opt-<eventId>-<optionId>`). When the tray is empty it shows a calm "no matters awaiting your decision" state. (2) **Staffing**: candidate cards and staffed-division rows **never display the director's hawkish/dovish orientation** (neither `lean` nor `disposition`) — only name, competence, skills, and fit; a staffed division shows a **Dismiss** button (data-testid `fire-<divId>`) calling `session.fire(divisionId)`; the hire cost is labelled against the operating budget. (3) **Committee legibility**: `PersuasionView` shows a one-line plain-language explanation of why members' preferred rates differ (data-testid `committee-legend`) and labels each dot/row as leaning higher or lower than the proposed rate (relative to the proposal, not revealing the hidden orientation). All player-facing strings use `t()`. AC: vitest-jsdom tests assert an escalation renders with its option buttons and resolving it removes it from the tray; no candidate or staffed row contains the hawk/dove labels; the Dismiss button fires `session.fire` and the division becomes unstaffed; and the committee legend renders.
- **SPEC-WEB-16** `[testable]` Real-time-with-pause time controls. A `useGameClock(session)` hook (`web/src/useGameClock.ts`) drives continuous play: while `playing`, a timer advances the session one month every `SPEED_MS[speed]` (slow/normal/fast). It hard-pauses while `session.escalations().length > 0` (a decision must be resolved first → `blockedByEscalation` true) and pauses once per FOMC meeting month so the Chair can act (tracked so resuming advances past it, not re-pausing). The shell renders a persistent clock strip with a play/pause toggle (`clock-toggle`), three speed buttons (`clock-speed-slow|normal|fast`), and a status line (`clock-status`); all strings via `t()`. AC: a vitest-jsdom test with fake timers asserts pressing play advances the date over time and pressing pause halts it; and that advancement is blocked while an escalation is pending.
- **SPEC-WEB-17** `[testable]` Committee members wear generated names that vary every game, and the homepage chart shows every series. `MeetingPanel`/`PersuasionView` render each member's display name via `session.npcName(member.id)` (SPEC-NAME-1), not the static localization name keys, so two sessions with different seeds show different committee rosters. `ChartsPanel` (SPEC-WEB-3) renders credibility on its own panel so the 0–100 credibility series never flattens the ~0–0.2 rate series; all four series stay visible. AC: a web test asserts the rendered committee names differ across two seeds; a ChartsPanel test asserts the credibility panel heading renders alongside the rate series.

## Macro dynamics
- **SPEC-PROD-1** `[testable]` A `state.vars.productivity` variable tracks total-factor productivity and drifts each month. A pure `applyProductivityDrift(state, params): GameState` reads `state.vars.productivity` (defaults to `1.0` if absent), multiplies by `(1 + params.monthly_drift_rate)`, and returns a new state with the updated value — it never mutates the input. `monthly_drift_rate` may be positive (growth) or negative (stagnation/decline). Params (`monthly_drift_rate`) live in `content/engine/productivity.json` (schema-governed). `Session.advance()` calls `applyProductivityDrift` each month after `applyMacroDynamics`. A test asserts that after N months productivity equals `1.0 * (1 + rate)^N` to within floating-point precision (IEEE-754 iterative multiplication and `Math.pow` may differ by ±1 ULP), that the function is pure (input state unchanged), and that `Session.advance(12)` produces a finite `productivity` var in the resulting state.
- **SPEC-LAG-1** `[testable]` A distributed-lag kernel translates the history of real-rate gaps into `state.vars.output_gap`. A pure `applyRateToOutputGap(state, trajectory, lagParams, realNeutralRate): GameState` reads up to N recent snapshots from `trajectory` (reverse-chronological order), computes `realGap[k] = (policy_rate[k] - expectations_anchor[k]) - realNeutralRate` for each, and returns a new state with `output_gap = Σ weights[k] * realGap[k]` where `weights` (from `content/engine/lags.json`) sum to 1 and follow an exponential decay with ~6-month half-life. `applyMacroDynamics` uses `state.vars.output_gap ?? realGap` for the Okun unemployment-equilibrium computation, so existing tests without `output_gap` are unaffected. Session.advance() calls `applyRateToOutputGap` before `applyMacroDynamics` each month.

- **SPEC-TERM-1** `[testable]` A `state.vars.long_rate` variable tracks the long end of the yield curve via exponential weighted moving average (EWMA) toward `policy_rate`. A pure `applyTermStructure(state, params): GameState` computes `λ = 1 - exp(-ln(2) / half_life_months)` from content, then `long_rate_new = (1 - λ) * long_rate + λ * policy_rate`; if `long_rate` is absent from state it defaults to `policy_rate` (cold-start). Returns a new state with `long_rate` updated; never mutates the input. Params (`half_life_months`) live in `content/engine/term-structure.json` (schema-governed). `Session.advance()` calls `applyTermStructure` each month after `applyMacroDynamics`. Tests assert: EWMA convergence (long_rate approaches policy_rate asymptotically), cold-start defaults to policy_rate, half_life_months=1 gives λ=0.5, purity, and Session.advance(12) produces a finite `long_rate` var.

- **SPEC-SIM-5** `[testable]` A pure `applyMacroDynamics(state, params): GameState` in `src/engine/dynamics.ts` evolves the macro state one month via a **real-rate transmission** model. All four var updates read the **prior** month's vars (a simultaneous update), so the step is order-independent and matches the calibration harness. Let `realRate = policy_rate - expectations_anchor` (the ex-ante real rate) and `realGap = realRate - real_neutral_rate`.
  - **Unemployment** mean-reverts toward a policy-implied equilibrium: `u_eq = unemployment_natural_rate + okun_coefficient * realGap`; then `unemployment += unemployment_adjustment_speed * (u_eq - unemployment)`. Tight policy (high real rate) raises equilibrium unemployment with a lag; returning to neutral lets unemployment recover — fixing the slice-1 model where a nominal-rate gap drove unemployment in one direction forever.
  - **Inflation** follows an expectations-augmented Phillips curve with momentum: `inflation = inflation_persistence * inflation + (1 - inflation_persistence) * expectations_anchor - phillips_slope * (unemployment - unemployment_natural_rate)`.

  Both are clamped (`inflation >= 0`, `unemployment` in `[0,1]`). The `expectations_anchor` (SPEC-CRED-4), `credibility` (SPEC-CRED-6), and `months_below_anchor` updates run inside this same simultaneous step. Macro params (`inflation_persistence`, `phillips_slope`, `unemployment_natural_rate`, `real_neutral_rate`, `okun_coefficient`, `unemployment_adjustment_speed`) live in `content/engine/dynamics.json`; expectations/credibility params live in `content/engine/credibility.json` (both schema-governed). `applyMacroDynamics` is called once per month inside `Session.advance()`; the slice-1 `applyMonthlySpiral` call is removed (its role is absorbed here). The model has a clean steady state at `inflation = expectations_anchor = target_inflation`, `unemployment = unemployment_natural_rate`, `policy_rate = target_inflation + real_neutral_rate` — a stable, winnable target.

- **SPEC-SHOCK-1** `[testable]` A seeded supply-shock term perturbs the Phillips curve inflation outcome each month. A pure `applySupplyShock(state, rng, params): GameState` draws a normally-distributed shock with mean 0 and standard deviation `supply_shock_sigma` from the caller's seeded `rng` instance (never `Math.random()` — SPEC-SIM-1), adds it to `state.vars.inflation`, and clamps the result at 0. The function is pure: input state is not mutated, the returned state has only `inflation` changed. Params (`supply_shock_sigma`) live in `content/engine/shocks.json` (schema-governed). `Session.advance()` calls `applySupplyShock` each month, passing the session's seeded RNG, after `applyMacroDynamics`. Tests assert: a non-zero sigma produces different inflation values across months (non-constant); sigma=0 produces no change; two Session runs with the same seed produce identical trajectories (determinism); purity (input unchanged); a large positive shock cannot make inflation negative (floor at 0).
- **SPEC-SHOCK-2** `[testable]` Supply shocks are distributionally correct, not merely non-constant: over a single seeded RNG stream of at least 2,000 `applySupplyShock` draws (with base inflation far above 0 so the floor never binds), the sample mean of the shock is within 3 standard errors of 0, the sample standard deviation is within ±10% of `supply_shock_sigma`, and the fraction of draws within 2σ of 0 lies in [0.93, 0.985] (normal coverage ≈ 0.954).

## Briefings
- **SPEC-BRIEF-1** `[testable]` A briefing content type: `schemas/briefing.schema.json` + loader (`src/content/briefings.ts`) + at least one committed briefing in `content/briefings/`. Each briefing exposes exactly three policy-scenario branches in order — raise, hold, lower — each with a forecast payload (`inflation_outlook`, `unemployment_outlook`, optional `growth_outlook`). The schema enforces branch order via `prefixItems`; the loader additionally validates order at runtime and throws `BriefingScenarioOrderError` if violated. `loadBriefing(id)` throws `BriefingNotFoundError` for an unknown id. The `name`/`desc`/scenario `name` fields are constrained to localization-key shape `^[a-z][a-z0-9_.]+$`. `npm run validate` accepts `content/briefings/1979_q3_stagflation.json` and rejects a briefing missing a scenario branch or a required forecast field.
- **SPEC-BRIEF-2** `[testable]` Briefing forecast quality scales with organizational investment: a pure `computeForecastNoiseScale(investment, params)` maps investment level to noise scale monotonically — higher investment yields lower noise (tighter forecasts); the floor `min_noise_scale` is always finite (no NaN at zero investment). A pure `applyForecastQuality(briefing, investment, params, rng)` returns a new `Briefing` with each scenario's forecast fields perturbed by uniform noise ±noiseScale; `unemployment_outlook` stays clamped to [0, 1]; `rng` is the caller's seeded generator (SPEC-SIM-1 — never `Math.random`). Params (`base_noise_scale`, `quality_slope`, `min_noise_scale`) live in `content/engine/forecast-quality.json` (schema-governed). The `investment` parameter is abstract (decoupled from the tech tree) so this SPEC does not block on the org/resource system.
- **SPEC-BRIEF-3** `[testable]` Each authored starting scenario beyond 1979 ships with a staff briefing: `content/briefings/2008_q4_crisis.json` and `content/briefings/2020_q1_pandemic.json` validate against `schemas/briefing.schema.json`, load via `loadBriefing`, and every localization key they reference resolves in `content/localization/en.json`. Forecasts are internally consistent with each scenario's demand-collapse regime: the raise scenario forecasts strictly lower `inflation_outlook` and strictly higher `unemployment_outlook` than the lower scenario. No real person names appear.

## Confirmation hearing
- **SPEC-HEAR-1** `[testable]` A confirmation-hearing game start: the incoming Chair answers a congressional committee's questions, and the answers deterministically select the starting scenario and shape the starting institutional posture. The mechanism is schema-governed (`schemas/hearing.schema.json`) and content-driven (`content/hearings/`). A pure `resolveHearing(answers, hearing): { scenarioId, modifiers }` scores each scenario additively from the chosen answers' `scenario_weights`; the scenario with the highest total score is selected (alphabetical tiebreak). `state_modifiers` from all chosen answers are accumulated. Committee members posing questions use generated, non-historical names. Works with the 1979 scenario and gains variety once multi-scenario content is available.
- **SPEC-HEAR-2** `[testable]` Hearing/scenario cross-content integrity: every scenario id referenced in any hearing answer's `scenario_weights` corresponds to a shipped file in `content/scenarios/`, and each of the three authored starting scenarios (`scen.1979_stagflation`, `scen.2008_gfc`, `scen.covid_2020`) is reachable — i.e. appears with a positive weight in at least one answer of `content/hearings/confirmation.json`. Enforced by test so a renamed or added scenario cannot silently break hearing resolution.

## Mandate evaluator
- **SPEC-MANDATE-1** `[testable]` A pure `onTarget(state, params): boolean` in `src/engine/mandate.ts` checks whether the economy satisfies the central bank's mandate. For `mandate_type === "single"`: returns true iff `|inflation - target_inflation| <= tolerance_band`. For `mandate_type === "dual"`: also requires `|unemployment - unemployment_target| <= unemployment_band`. Params (`target_inflation`, `tolerance_band`, `mandate_type`, `unemployment_target`, `unemployment_band`) live in `content/engine/mandate.json` (schema-governed). `onTarget` is called inside `Session.proposeRate()` to supply the `onTarget` field to `applyMeetingOutcome`, wiring the third SPEC-CRED-1 credibility lever.

## Doctrine

- **SPEC-DOCT-1** `[testable]` A doctrine-adoption mechanic: a doctrine is content-defined (schema + `content/doctrines/`) with declared standing effects (additive modifiers to state vars) and a flip-flop cost. `adoptDoctrine(state, doctrine): GameState` records the doctrine in `state.flags` and applies its standing effects; no credibility cost on first adoption. `abandonDoctrine(state, doctrine): GameState` reverses the standing effects and deducts `flip_flop_cost` from credibility (clamped to [0, 100]). `adoptDoctrine` throws `DoctrineAlreadyAdoptedError`; `abandonDoctrine` throws `DoctrineNotAdoptedError` if preconditions are not met. At least one example doctrine in `content/doctrines/`; `npm run validate` green.
- **SPEC-DOCT-2** `[testable]` Dot-plot doctrine: an adoptable transparency reform (content/doctrines/dot_plot.json) that applies a per-meeting anchoring credibility bonus while adopted and a spread-exposure credibility cost proportional to the spread of member preferred rates when the spread exceeds a threshold. A dissent multiplier amplifies the cost when at least one member dissents. When not adopted, neither effect applies. Effects route through `applyDotPlotMeetingEffects` called in `Session.proposeRate()` after `applyMeetingOutcome`. `npm run validate` green; `// SPEC-DOCT-2` tests cover bonus, exposure, dissent multiplier, no-op when not adopted, threshold gate, and loader.
- **SPEC-DOCT-3** `[testable]` A third adoptable doctrine ships as pure content: `content/doctrines/gradualism.json` validates against `schemas/doctrine.schema.json`, uses only generic schema fields (standing effects + flip-flop cost, no meeting hook, no bespoke engine code), appears in `loadDoctrineCatalog()`, resolves via `getDoctrine("doctrine.gradualism")`, and its localization keys resolve in `content/localization/en.json`.

## Repo metadata
- **SPEC-META-1** `[testable]` Repo state is generated, not hand-maintained: `tools/gen-state.ts` emits `STATE.json` deterministically from tree-only facts (slice plan checkbox tallies counting all `- [ ]`/`- [x]` lines with `null` for checkbox-less plans; SPEC↔test mapping via `tools/lib/spec-parse.ts`; content file counts) — never from git history or the clock, so `state:check` (regenerate + byte-compare, wired as an explicit `build-and-test` CI step and into `npm run check`) is deterministic on any checkout depth. Human intent lives only in `state.manual.json` (schema-governed by `schemas/state-manual.schema.json`, Ajv-validated by `state:check`), which no generator ever writes.
- **SPEC-META-2** `[testable]` Project docs are generated, not hand-maintained: `tools/gen-docs.ts` emits README managed sections (between `<!-- gen:* -->` marker pairs; missing/malformed markers fail loudly), `docs/content-reference.md` from the JSON Schemas, and `docs/traceability.md` (SPEC → tag → section → tests → PR). Plain generation is tree-pure — existing PR cells are preserved input and git history is never read — so `docs:check` (regenerate + byte-compare, wired as an explicit `build-and-test` CI step and into `npm run check`) is deterministic at any checkout depth; only `--enrich` (workflow-only) fills empty PR cells from `(#N)` squash subjects without ever altering a non-empty cell.
- **SPEC-META-3** `[testable]` The routine agent's decision procedure is code, not prose: a pure `plan(state, manual, live)` in `tools/routine-dryrun.ts` emits the fire's ActionPlan (drain oldest `agent-task` issue / merge approved+green PRs / fix REQUEST_CHANGES PRs with a hard STUCK boundary at 3 rounds / park on manual gates / zero-trace idle), with an auditable trace line per decision. `--no-network --fixtures` mode makes the procedure CI-testable; the scheduler prompt (docs/routine-prompt-v3.md) is a thin wrapper that executes the plan and adds no decision logic.
