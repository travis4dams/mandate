## What & why

<!-- One or two sentences. Link the SPEC id(s) this PR implements. -->
Implements: SPEC-

## Checklist
- [ ] Added/updated a requirement in `spec/requirements.md` if behavior changed
- [ ] Wrote the failing test first, referencing the SPEC id in a comment
- [ ] `npm run check` passes locally (typecheck + validate + spec:trace + test)
- [ ] No game content hardcoded in engine code (content lives in `content/`)
- [ ] No new player-facing strings outside `content/localization/`
